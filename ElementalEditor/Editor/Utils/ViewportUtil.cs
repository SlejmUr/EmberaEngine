using System;
using System.Collections.Generic;
using OpenTK.Graphics.OpenGL;
using OpenTK.Mathematics;
using EmberaEngine.Engine.Rendering;
using ImGuiNET;

namespace ElementalEditor.Editor.Utils
{
    static class ViewportUtil
    {
        //private void DrawViewportTools(float toolbarSize)
        //{
        //    const float buttonSize = 40f;
        //    Vector2 toolbarSize = new Vector2(ImGui.GetContentRegionAvail().X - 1.5f, ToolbarHeight);

        //    PushToolbarStyle();
        //    ImGui.SetCursorPos(new Vector2(1.5f, ToolbarHeight));
        //    ImGui.BeginChild("##VIEWPORT_TOOLS", new System.Numerics.Vector2(-1, toolbarSize), false, ImGuiWindowFlags.NoScrollbar | ImGuiWindowFlags.AlwaysUseWindowPadding);

        //    DrawResolutionDropdown();
        //    DrawPlayPauseButton(buttonSize);
        //    DrawSettingsButton();

        //    ImGui.EndChild();
        //    PopToolbarStyle();
        //}





    }
}
